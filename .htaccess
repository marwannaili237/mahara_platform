<IfModule mod_rewrite.c>
    RewriteEngine On
    # This .htaccess is now in /var/www/html/
    # Rules here might apply if not overridden by .htaccess in subdirectories
    # or if requests don't match DocumentRoot or Alias directly.

    # Example: If you had specific global rules, they could go here.
    # For now, we rely on the Alias for /backend and DocumentRoot for frontend.
</IfModule>
